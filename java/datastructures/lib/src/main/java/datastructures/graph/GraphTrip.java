package datastructures.graph;

import java.util.ArrayList;

public class GraphTrip(Graph<String> graph, ArrayList<String>) {
    int totalCost = 0;
    Graph<String> vertices = graph.getVertices();
    ArrayList<String> city = new ArrayList<>();
    for(int i = 1; i city.length; i++) {
      boolean isConnected = false;

      if(edge.connect.value.equals(city[i])) {

        flight = edge.destination;

        connect = true

      }

    }
    if (!= connect)
      return null;
    else {

return totalCost;
}
  }
